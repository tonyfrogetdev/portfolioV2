import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Tony FROGET ~ Développeur Web Full Stack JavaScript",
  description:
    "Découvrez le portfolio de Tony FROGET, un développeur spécialisé en JavaScript, expert en création d'applications web modernes et performantes.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
